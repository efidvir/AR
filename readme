Collecting workspace information

To run the 

app.py

 application using ngrok, follow these steps:

## Running the Application

### Prerequisites
- Python 3.x
- Flask
- Flask-SocketIO
- ngrok

### Installation

1. **Clone the repository:**
    ```sh
    git clone https://github.com/your-repo.git
    cd your-repo
    ```

2. **Install the required Python packages:**
    ```sh
    pip install Flask Flask-SocketIO
    ```

3. **Install ngrok:**
    Download and install ngrok from [ngrok.com](https://ngrok.com/).

### Running the Application

#### Version 1

1. **Navigate to the 

v1

 directory:**
    ```sh
    cd v1
    ```

2. **Run the Flask application:**
    ```sh
    python main.py
    ```

3. **Start ngrok to tunnel the local server:**
    ```sh
    ngrok http 5000
    ```

4. **Access the application:**
    Use the URL provided by ngrok to access the application.

#### Version 2

1. **Navigate to the 

v2

 directory:**
    ```sh
    cd v2
    ```

2. **Run the Flask application:**
    ```sh
    python app.py
    ```

3. **Start ngrok to tunnel the local server:**
    ```sh
    ngrok http 5001
    ```

4. **Access the application:**
    Use the URL provided by ngrok to access the application.

### Notes
- Ensure that you have the necessary permissions to run ngrok and access the local server.
- The default ports used are 5000 for version 1 and 5001 for version 2. Adjust the ngrok command if you change the ports.